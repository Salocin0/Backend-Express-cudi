import { Router } from "express";

const cartRouter = Router()

cartRouter.get("/:id",) // populado
cartRouter.get("/",) //populado
cartRouter.post("/",)
cartRouter.put("/:id") // vaciar el carrito, agregar distintos elementos


export default cartRouter